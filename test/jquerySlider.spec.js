import * as slider from '../src/jquerySlider.js'

let sliderNode = document.createElement('div');
$test = $('div');
document.body.append(sliderNode);

/* describe('JQuery plugin slider', () => {


    it('can create', () => {
        $test.slider();
        expect($test.hasClass('1slider')).toBeTruthy();
        expect(1).toBe(2);

    })
}) */